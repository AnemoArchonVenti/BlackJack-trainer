{
  "name": "blackjack-trainer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/build-seo.js",
    "build:app": "vite build",
    "preview": "vite preview",
    "fonts": "node scripts/fetch-fonts.cjs",
    "test": "node --test \"src/**/*.test.js\"",
    "images": "node scripts/make-images.cjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "svelte": "^5.57.0",
    "vite": "^8.3.0"
  }
}
